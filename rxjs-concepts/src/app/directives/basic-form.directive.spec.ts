import { BasicFormDirective } from './basic-form.directive';

describe('BasicFormDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicFormDirective();
    expect(directive).toBeTruthy();
  });
});
